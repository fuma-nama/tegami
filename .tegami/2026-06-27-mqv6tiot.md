---
packages:
  npm:tegami:
    type: patch
    replay:
      - exit prerelease: tegami
---

## Generate dist tag for npm in prerelease mode

`npm publish` now requires `tag` for prerelease versions.
