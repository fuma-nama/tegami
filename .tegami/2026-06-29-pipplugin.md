---
packages:
  "npm:tegami": minor
---

### Add pip plugin

Tegami now discovers Python packages from `pyproject.toml`, supports uv workspaces, bumps dependency ranges, runs `uv lock` after versioning, and publishes to PyPI with `uv publish`.
