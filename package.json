{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "oxlint",
    "lint:format": "oxfmt --check",
    "format": "oxfmt",
    "test": "vitest",
    "types:check": "turbo run types:check",
    "tegami": "pnpm --filter tegami build && node scripts/tegami.mjs",
    "changelog": "pnpm --filter tegami build && node scripts/tegami.mjs",
    "version": "pnpm --filter tegami build && node scripts/tegami.mjs version",
    "release": "pnpm --filter tegami build && node scripts/tegami.mjs publish"
  },
  "devDependencies": {
    "oxfmt": "^0.54.0",
    "oxlint": "^1.69.0",
    "turbo": "^2.9.18",
    "typescript": "6.0.3",
    "vitest": "^4.1.8"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@11.6.0"
}
